mutation UpdateExerciseComment(
  $id: ID!
  $comment: String!
  $contributors: [ID!]!
) {
  updateExerciseComment(
    id: $id
    comment: $comment
    contributors: $contributors
  ) {
    ...ExerciseComment
  }
}