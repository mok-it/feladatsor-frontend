query stats {
  globalStats {
    checkedExerciseCount
    totalExerciseCount
    exerciseHourlyCount {
      count
      hour
    }
    userLeaderboard {
      rank
      submittedExerciseCount
      user {
        id
        name
        avatarUrl
      }
    }
  }
}
