mutation CreateExerciseComment(
  $exerciseId: ID!
  $comment: String!
  $contributors: [ID!]!
) {
  createExerciseComment(
    exerciseId: $exerciseId
    comment: $comment
    contributors: $contributors
  ) {
    ...ExerciseComment
  }
}
